{{page-title "Board"}}

<nav class="navbar navbar-light bg-light">
<button class="btn btn-outline-success my-2 my-sm-0"{{action "initialize"}}>Initialize</button>
  <l>{{@model.employee.email}} </l>
<button class="btn btn-outline-danger my-2 my-sm-0" {{action "disconnect"}}>Logout</button>
</nav>

<div class="container p-3 my-3 bg-dark text-white"style="border-radius: 10px;">
  <h1>Gestion du Store</h1>
  <nav class="navbar navbar-light" style="background-color: #FFFFFF; border-radius: 10px;  ">
    <LinkTo class="btn btn-outline-success my-2 my-sm-0" @route="section">Modifier Section</LinkTo>
    <LinkTo class="btn btn-outline-success my-2 my-sm-0" @route="products">Modifier Produit</LinkTo>
    {{outlet}}
  </nav>

  <div style="margin-top: 10px">
    <ul>
      {{#each @model.orders as |order|}}
        <li style="margin-top: 10px">
          <LinkTo @route="order" @model={{order.id}}>
            {{order.dateCreation}} <span class="ui circular label">{{order.count}}</span>
          </LinkTo>
          <!--<button class="btn btn-outline-danger btn-sm float-right" {{action ""}}>Supprimer</button>
          <button class="btn btn-outline-warning btn-sm float-right"style="margin-right: 20px" {{action ""}}>Modifier</button> -->


        </li>
      {{/each}}
    </ul>
  </div>
</div>
